// EMBER Integer Exact CSG - Implementation file
// This file exists primarily to ensure the library compiles as a static library.
// Most of the implementation is header-only due to template usage.

#include <ember/ember.hh>

// Explicit template instantiation is not needed since all code is inline/header-only.
// This file serves as a compilation unit for the static library target.

namespace ember
{
// Version information
static constexpr int VERSION_MAJOR = 0;
static constexpr int VERSION_MINOR = 1;
static constexpr int VERSION_PATCH = 0;
} // namespace ember
